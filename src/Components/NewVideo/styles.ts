import styled from 'styled-components'

export const Wrapper = styled.div`
  h1 {
    font-size: 1.25rem;
    text-align: left;
  }
  .add {
    display: flex;
    height: 50px;

    input {
      width: 600px;
      outline: none;
    }

    button {
      border-radius: 0;
      outline: none;
      width: 145px;
    }
  }
`
